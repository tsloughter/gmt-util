%%% -*- mode: erlang -*-

%% Depends
{deps, [lager]}.

%% Erlang compiler options
{erl_opts, [debug_info, warnings_as_errors
            , {parse_transform, lager_transform}
           ]}.

%% EUnit options
{eunit_opts, [verbose]}.
